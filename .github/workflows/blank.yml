name: ArchivingImageDB

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Generate dataset archive
      run: |
        zip -r moss-db.zip ./dataset
